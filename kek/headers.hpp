#include <filesystem>
#include <vector>
#include <string>
#include <fstream>
#include <vector>

int MyIteraror(std::string, bool pass);
int MyReader(void);
int AVSearch(std::string, char *);
int AVKiller(char *);
int AVQuarantine(std::string);
int AVZip(char *);
int AVZipPas(char *);
int serverStart(void);
int Interface(std::string, int);
int callback(std::string);
int rdCallback(std::vector<std::string>);
void scanCallback(void);